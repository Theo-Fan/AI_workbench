# 四个生活工作页改造概览

## 当前入口与架构决策

当前重构版以 `creator-workspace.html` 为唯一的 UI 与交互基准。Vite 通过简洁的 `/workspace.html` 原样提供页面，同时保留 `/legacy/creator-workspace.html` 兼容别名；首页和领域入口统一重定向到对应的 hash 页面，因此不会出现“重构版功能缩水或视觉不一致”的双套界面问题。

页面默认经 `apps/api` 的 Fastify API 读取和保存完整工作区快照，快照存储于 SQLite 的 `workspace_documents.legacy_snapshot`，带独立乐观锁版本；天气请求由 Fastify 代理，Zotero 继续经本机只读 Agent 访问。直接打开 `8788` 仍保留旧版文件夹授权与 localStorage 回退入口。

## v4.0 数据与交互稳健性补强

- 统一迁移入口将旧数据升级到 v4.0，并校验关键结构与不安全字段；导入失败不会覆盖当前数据。
- 任务具备创建、更新和截止日期元数据；首页与每日计划镜像任务按语义去重，跨日/跨周状态会自动归档。
- 保存操作串行化，避免连续点击造成异步写入交错；同步冲突仍保留三种用户决策路径。
- 页面支持 hash 深链与浏览器前进/后退，导航、任务复选框和弹窗补齐键盘与 ARIA 行为。
- 移动端首页完成度区与健身热力图在 390px 视口下收敛布局，避免关键计划被推到首屏之外。

## 已完成

- 重新设计了 `每日计划`、`健身打卡`、`AI 学习` 与 `英语学习`，统一为与科研面板一致的工作区视觉语言：浅灰蓝背景、白色内容面板、细边框、克制蓝色强调和紧凑数据层级。
- 为四页引入页面级布局与样式，不影响科研、内容创作等既有页面的通用卡片与列表样式。
- 每页增加了顶部工作区概览，优先显示当前进度、下一项任务和主要操作，减少无目的留白。
- 每日计划：增加全天完成度、时段任务分区、下一项待办、周复盘编辑区及结转入口。
- 健身打卡：将抽象的训练概览环替换为“已完成项 / 总计划 + 完成百分比 + 进度条”的清晰完成度摘要；训练热力图升级为近 6 个月的月度网格，浅绿色表示计划日，绿色勾表示已有训练记录的完成日。
- AI 学习：增加学习闭环概览、任务进度、下一项学习任务和更清晰的资源库列表。
- 英语学习：增加百日挑战概览、练习节奏说明与“完成今日挑战”动作。仅当当天所有英语任务完成时可记录挑战；同一天重复操作不会重复累计。

## 兼容性与决策

- 保留了现有任务、训练计划、训练日志、学习资源、回收站和导入导出数据结构。
- 保留任务复选框、回车新增、双击编辑、训练记录弹窗和删除行为。
- 英语挑战新增 `lastCompletedDate` 字段，用于当天去重；旧数据会通过默认合并自动获得该字段。
- 总览和每日计划新增同一组“今日专项行动”：自动汇总当天的健身计划和英语练习；英语勾选会即时同步，健身计划沿用训练记录弹窗完成，已完成项可直接撤销。

## 验证

- 内联 JavaScript 语法检查通过。
- 本机工作台服务已恢复，`http://127.0.0.1:8788/creator-workspace.html` 返回 HTTP 200。
- Playwright 实测四页在 1440px 桌面端和 390px 移动端均无运行时错误、无横向溢出。
- 英语挑战逻辑验证通过：全部任务完成后挑战天数递增，同日再次触发不会重复增加。
- 最终回归：健身页实际渲染 6 个热力图月份、绿色完成日和计划日状态；总览/每日计划专项行动均存在；英语专项任务的点击同步与状态恢复验证通过；1440px 与 390px 下没有页面运行时错误或横向溢出。
- 深色模式修复：为热力图计划日、完成日、图例和进度条补充高对比度深色配色，完成勾在深色背景下可清晰识别。
- 健身页布局修复：收窄完成度说明的 CSS 选择器，避免误伤内部进度条标签，修复右侧文字被挤成竖排的问题。
- 天气修复：新增本地 `/api/weather/forecast` 与 `/api/weather/geocode` 代理，由 `zotero-bridge.js` 转发 Open-Meteo 请求，绕过页面 CSP 对跨域请求的拦截，并保留缓存与失败降级。
- 健身页顶部右侧重新组织：将“训练热力图”重命名为“打卡记录”，移入 Hero 右侧替代原完成度文字；下方不再重复显示热力图，移动端自动切换为单列。
- 文献面板“打卡记录”热力图修正：空白格与面板背景由固定浅灰改为基于主题变量的颜色，深色模式下补齐四档阅读强度色阶（#285a39 → #367c48 → #4ba95d → #70cf7e，由弱到强），并加上内描边提升可读性；移除按月份独立计算、窄屏换行的旧布局，改为六个月单行连续时间轴，统一按面板宽度缩放单元格，保证星期刻度对齐；保留悬浮详情、焦点提示与阅读日志数据。
- 文献热力图进一步修正为贡献图风格：月初占位格（lead cells）在深色模式下修复为完全透明（此前被主题样式覆盖）；未来日期不再渲染格子，8 月等当前月只显示到当天为止，与参考布局一致。
- 文献热力图月份标签对齐修正：为 `.hm-month-label` 设置与当月格子区域同宽的内联宽度，并补充 `min-width: max-content` 与左右 2px 内边距，避免 8 月等列数较少的月份出现文字贴边、视觉上偏左的问题。
- 8 月标题进一步改为按完整月份 6 列日历跨度居中：未来日期仍不渲染，但参与标签定位和完整月份布局计算；实际可见日期保持当前日期截止，不影响其他月份的格子密度。

## 验证

- 文献热力图修正后：桌面 1440px 与移动 390px 下均为 6 个月、无横向溢出、无页面运行时错误。
- 深浅主题四档色阶实测：浅色 `#a8dca9/#69bd70/#368b45/#1f6931`，深色 `#285a39/#367c48/#4ba95d/#70cf7e`；空白格在两种主题下均与面板背景可区分。
- 贡献图风格修正后：各月 lead 占位格在深色下完全透明（futureVisible=0）；8 月只渲染到 8 月 6 日（12 格、2 列），未来日期不再生成格子。

## 后续建议

- 可继续为 AI 学习和英语学习增加历史记录与周期趋势，形成长期复盘，而不只展示当前任务。
- 若需要真正的“明日结转”历史追踪，应另行引入按日期存储的计划模型并明确迁移旧任务数据。

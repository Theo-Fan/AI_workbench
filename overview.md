# 本轮工作概览

## 已完成
- Reading Activity 热力图改为参考图风格：浅灰空格与四级绿色强度，单日打卡文献越多颜色越深。
- 热力图与本地阅读记录均限定为近 6 个自然月，启动页面和新增打卡时都会清理更早记录。
- 新增文献打卡以规范化标题校验已读记录，忽略大小写、空格及常见标点，避免同一文献重复打卡。
- 打卡日期限制在近六个月内，阻止录入过期或未来日期。
- 打卡提交前新增 Zotero 已读检查：匹配到当前 Zotero 文献时读取其 AI 管家/AI 总结笔记；已有阅读笔记或状态读取失败均不写入本地记录，提交按钮在检查期间禁用。
- 热力图按自然月份显示 6 个月份标签，日期格固定为 13×13px 的小圆角正方形，沿用浅灰到深绿的四级填充，并在窄屏横向滚动保持可读性。
- 热力图改为按月份分组、整体自适应缩放：月份不再使用独立方框，去掉边框与滚动条，网格随面板宽度等比缩放，6 个月完整数据一次可见、无需左右拖动；窄屏下月份改为等宽纵向排列。
- 热力图进一步压缩：整体缩小面板与间距，日期格统一为相同像素尺寸，月份等宽对齐；渲染后按实测容器宽度精确校准，窗口缩放时自动重新适配，始终无需横向滚动。
- 修复 8 月显示不完整：原宽度分配按月份平均、再用最大列数反推格子，窄窗口下 6 列月份（8 月）被 `overflow: hidden` 裁剪。现改为按全部月份总列数统一分配格子尺寸，月份按自身列数占宽，6 列月份自然更宽；窄屏（900–800px）自动换行成两行，保证 8 月 31 天在任意宽度完整可见、无需横向拖动。
- 热力图改为只渲染到今天：未来日期不再生成格子，每天打开页面自动多出今天的格子；6 个自然月窗口保持不变，进入 9 月后 3 月自动移出（月份标签随之滚动）。
- 热力图本月预留空白：当前月完整显示整月形状（今天之后的日期以透明占位格呈现），格子尺寸按总列数自适应缩放，仍保持所有宽度无横向滚动。
- 热力图悬停提示升级：有打卡记录的绿色日期格现使用自定义详情浮层，显示日期、篇数及当日全部文献名称；浮层随鼠标定位并避让视口边界，特殊字符标题已正确转义。
- 热力图详情支持键盘聚焦，提供 ARIA 标签与可见焦点状态；有记录日期的悬停光标改为正常的指针，避免出现问号光标。
- 文献类型 SVG 环形图和右侧图例实现双向联动：悬停或键盘聚焦任一扇区/图例时，对应扇区轻微放大，图例同步突出，非当前项适度弱化。
- 修复知识图谱生成按钮被空状态遮罩拦截的问题；生成按钮现在可接收点击，随后完成画布与运行态挂载。

## 验证
- 内联脚本语法检查通过：1 个脚本。
- 本地 Zotero 桥接健康检查成功，工作台页面 `http://127.0.0.1:8788/creator-workspace.html` 返回 HTTP 200。
- 浏览器回归通过：热力图事件委托能显示 `Attention Is All You Need` 的当日详情，图例可反向激活对应环形图扇区；1440/900/390px 下页面均无横向溢出，内联脚本无语法错误。

## 说明
- 改动集中在 `creator-workspace.html`；Zotero 数据仍保持只读，不会写回原始文献库。

# 文献知识图谱优化大纲与实施记录

## 问题检查

1. **节点拖拽不跟手**
   - 指针移动只更新了节点的 `x/y`，力导向循环下一帧仍使用按下时保存的 `fx/fy`，导致节点被持续拉回原位。
   - 释放节点时没有保留拖动速度，邻域响应偏僵硬，缺少 Obsidian 图谱式的动态重组感。
2. **画布表达不够聚焦**
   - 固定缩放系数无法适应不同画布宽度，宽屏下图谱聚集在中央小区域。
   - 高权重节点全部显示标签，缺少碰撞避让，标签重叠会遮挡关系结构。
   - 统计卡片占用过多纵向空间，右侧洞察没有围绕“当前选中节点”形成主任务。
3. **节点点击信息不足**
   - 只有文献节点可以触发动作，标签、作者和关键词点击后没有解释。
   - 用户无法快速理解节点类型、连接强度、关联文献和关键邻居。

## 优化大纲

### A. 交互物理层

- 拖动过程中同步更新 `fx/fy`，确保节点位置与指针逐帧一致。
- 引入图谱热度衰减，拖动时重新激活局部力导向，释放后自然收敛。
- 记录拖动速度并在释放时赋予轻微惯性，使节点和邻域连续响应。
- 区分节点拖动、画布平移、节点点击，避免动作互相误触。

### B. 画布表达层

- 根据实际节点边界自适应计算居中与缩放，不再使用固定缩放档位。
- 整体保持一个统一圆形边界，在圆内根据 Zotero 标签、关键词覆盖度和关联文献自动生成内容主题簇。
- 文献、作者、标签和关键词在每个内容主题中混合分布，不再按节点类型划分区域。
- 优先使用结构化 Zotero 标签作为主题名称，标签不足时再使用高频关键词补充；主题标识显示对应文献数。
- 主题命名遵循“研究对象-方法”语义：例如 `SDs-RL` 表示用强化学习研究社会困境；纯方法主题直接使用 `RL`、`MARL`、`LLMs`，不再添加无信息增益的 `AI-` 或 `ML-` 前缀。
- RL、MARL、LLMs、NLP 被视为独立方法主题；由不同 AI/ML 上位标签产生的同名主题会在布局阶段自动合并。
- SSDs 采用具体概念优先：明确出现 Sequential、Markov 或 intertemporal social dilemma，以及 SSD/SSDs 缩写的文献，统一进入 `SSDs-MARL`；其余社会困境文献统一进入 `SDs-RL`，不再按单篇文献中的方法词拆分。
- 主题标题会在主题簇的上、下、左、右候选位置中寻找离节点最远的位置，再使用独立胶囊标签绘制。
- 各主题簇拥有低对比度范围底纹和柔和的位置约束，既保持圆形整体，也避免所有节点重新堆叠到中心。
- 精简统计条和工具栏，增加“实时力导向”状态提示。
- 调整背景、节点光晕、连线透明度和选中态，强化层级而不过度装饰。
- 只展示高权重标签，并加入标签碰撞检测；选中与悬停标签拥有最高优先级。
- 保持浅色、深色、桌面与移动端视觉一致。

### C. 节点洞察层

- 点击文献、标签、作者或关键词均生成节点介绍。
- 展示节点类型、直接连接数、关联文献数、权重和关键邻居。
- 文献节点补充作者、日期和摘要要点，并提供“打开文献详情与阅读笔记”入口。
- 选中节点及其一阶邻域持续高亮，未关联节点自动弱化。
- 单击图谱中的文献节点会自动读取对应 Zotero 条目及子笔记，并同步填充上方“文献详情与笔记”；加载重绘后会恢复原文献节点的选中与邻域高亮。
- 连续快速选择不同文献时使用请求版本保护，仅最后一次选择可以更新详情面板，避免旧请求覆盖新选择。
- 全局预览时普通关系透明度降至约 4%，强关系约 12%；悬停、选中或拖动节点后，直接关系恢复到 42%–88%，避免 900 条连线长期干扰节点阅读。

### D. 验证与兼容

- 校验内联 JavaScript 和桥接脚本语法。
- 使用真实 Zotero 数据验证节点点击、拖拽、文献详情联动和动态图布局。
- 检查 390px 移动端单列布局、侧栏顺序和画布尺寸。
- 检查浏览器 warning/error 日志。

### E. 大型文献库与图谱范围

- Zotero 只读桥接支持 `start + limit` 分页，并返回文献总数与是否还有下一页。
- 工作台连接 Zotero 后会在后台自动连续分页，默认加载当前范围内的全部文献，不再要求用户逐页点击。
- 收藏夹切换或搜索后，同样会自动读取该范围的全部结果，并按 Zotero key 去重。
- 文献库标题实时显示“已加载数量 / 当前检索总数”。
- 图谱可按最近 50、100、200 篇、全部文献或当前文献邻域构建。
- 图谱默认显示全部文献并保证每篇文献都有节点；文献数量较大时仅压缩低权重概念节点和冗余关系。
- 当图谱超过 360 个节点时，斥力计算自动切换为固定邻域采样，避免全量两两计算导致拖拽卡顿。
- 点击节点进入聚焦状态；点击画布空白区域会清除选中、隐藏提示并恢复全部节点与关系预览。
- 关键词优先识别技术短语及其缩写，例如 Sequential Social Dilemmas (SSDs)、Multi-Agent Reinforcement Learning (MARL) 和 Large Language Models (LLMs)，并过滤低信息量孤立词。
- 主题内部保持较紧凑的螺旋间距；超过 360 个节点时采用采样斥力，在可选中性与整体聚合感之间保持平衡。

## 已完成

- 上述 A、B、C、D 四部分均已实现。
- 节点视觉进一步改为四类独立语义：文献使用圆角资料卡片、标签使用带孔菱形、作者使用头像轮廓、关键词使用轨道圆；画布与侧栏图例保持一致。
- 全量图谱已改为圆内内容主题聚类；类别会根据当前收藏夹、搜索条件和图谱范围自动重新计算。
- 拖拽采用轻量平滑追随：节点每帧向指针目标移动 44%，单帧最大位移由 18 提高到 28 个图谱单位；鼠标释放后仍短暂降低弹簧力并渐进恢复，保留较慢、自然的回弹。
- 拖动节点时会高亮其一阶邻居和直接关系：邻居节点放大并增加轮廓/光晕，直接连线提高透明度、线宽和发光强度，无关节点进一步弱化。
- 文献库自动分页、总数显示与图谱范围筛选均已实现；范围筛选会立即重建统计、节点和关系。
- 图谱已使用 126 篇真实 Zotero 文献验证，全部 126 个文献节点均已绘制。
- 作者、关键词与文献节点点击介绍均已验证。
- 文献节点可以从图谱介绍继续打开 Zotero 文献详情与 AI 阅读笔记。
- 桌面端节点拖拽、释放惯性和邻域重排通过回归。
- 390px 移动端画布与节点介绍采用单列布局，未出现横向溢出。

## 验证结果

- `creator-workspace.html` 内联 JavaScript 语法通过。
- `zotero-bridge.js` 语法通过。
- `git diff --check` 通过。
- 浏览器控制台无 warning 或 error。
- 真实 Zotero 库共 126 篇：首次连接后自动完成全部后台分页，显示“已加载 126 / 126”，页面不再显示“加载更多”按钮。
- 分页接口的 `start=0` 与 `start=10` 返回不同文献 key，且共同返回总数 126，验证无重复翻页。
- 图谱“最近 50 篇”和“最近 200 篇”范围切换通过；加载 100 篇时后者按实际 100 篇构建，加载完成后按实际 126 篇构建。
- 本地只读桥接服务运行于 `http://127.0.0.1:8788/creator-workspace.html`。

# Zotero 文献笔记阅读功能调整

## 已完成

- “文献详情与笔记”现在展示文献基本信息与对应的“AI 阅读笔记”。
- 支持通过笔记标题、标签或正文首部识别 `AI 管家 - *` 与 `AI 总结 - *` 数据，并在界面统一映射为“AI 阅读笔记”；当两者同时存在时优先使用 `AI 管家 - *`。
- 阅读笔记预览限制为约 520 字符和 7 行，避免超长内容撑开卡片。
- 新增“阅读完整笔记”按钮与沉浸式阅读器，完整保留 Zotero 笔记内容。
- 阅读器支持系统全屏、退出全屏、Esc 关闭、长内容滚动和移动端适配。
- 阅读器使用项目内置的 MathJax SVG TeX 引擎，公式采用接近 Computer Modern 的学术字形，支持常见 LaTeX 行内与块级公式，并兼容 Zotero `.math` 节点及跨行 `\\displaystyle` 公式。
- 对 Zotero 笔记 HTML 做白名单清理，移除脚本、事件属性与危险链接。
- `zotero-bridge.js` 增加子笔记标签透传，兼容通过标签标识“AI 管家”的情况。

## 验证结果

- 工作台内联 JavaScript 与 Zotero 桥接脚本语法通过。
- 本地桥接 HTTP 页面加载测试通过。
- 无头 Chrome 验证：预览 521 字符并限制 7 行；阅读器打开后完整保留 1,417 字符内容；浏览器全屏能力可用。
- 缺失笔记提示、标签识别、危险 HTML 清理和阅读器关闭状态复位均通过。
- 使用真实 Zotero AI 管家笔记回归：226 个公式全部生成 SVG TeX 输出，其中 37 个显示公式正确识别，未残留原始 `\\displaystyle` 文本；公式不再依赖系统字体回退。
- 行间公式现占满阅读栏并按几何中心对齐：桌面端与 390px 移动端短公式左右误差不超过 0.02px；移动端 14 个超宽公式保留从左侧开始的横向滚动，不会裁掉公式开头；189 个行内公式维持原有行内排版。

## 使用说明

启动 Zotero 与本地桥接后进入“科研 → 文献”，选择文献即可查看基本信息和 AI 阅读笔记预览；点击“阅读完整笔记”进入阅读器。公式组件已经随项目本地提供，不再依赖外部 CDN。

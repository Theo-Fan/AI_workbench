# 工作台架构优化记录

## 已落地（v4.0）

- 数据模型加入显式 `schemaVersion`、ISO 周键和日期/周状态；旧 JSON 在启动、冲突加载和导入时统一迁移。
- 每日状态会在跨日时快照并重置，健身计划在跨周时归档；每日快照最多保留 90 天。
- 首页任务统计按规范化语义键去重，避免每日计划与首页镜像任务重复计数。
- 任务补齐 `createdAt`、`updatedAt`、`dueDate` 等元数据；结转只保留未完成项并记录来源，不把已完成项误放入回收站。
- 保存请求串行化，连续操作会合并为顺序写入；文件冲突仍由用户选择远端、本地副本或强制覆盖。
- 导入增加 20 MB 大小限制、结构校验和原型污染字段拦截，失败时保持当前数据不变。
- 页面导航支持 URL hash 深链、刷新和浏览器前进/后退；侧栏和任务复选框支持键盘操作及 ARIA 状态。
- 弹窗增加隐藏状态、焦点回收、Escape 关闭和 Tab 焦点陷阱；移动端首页进度区与健身热力图优化为窄屏布局。

## 前后端分离重构：阶段 1 已完成

- 建立 `apps/api`、`packages/contracts` 和工作区级 npm 配置。
- 使用 Fastify + TypeScript + better-sqlite3 建立 SQLite API 基础服务。
- 增加 SQLite schema 初始化、任务表索引、软删除、版本字段和审计表。
- 增加工作区与任务的读取、新增、更新、软删除接口。
- 增加 Zod 共享契约与版本冲突返回（HTTP 409）。
- 增加旧版 `data/workspace.json` 到 SQLite 的幂等导入脚本。
- 保留 `creator-workspace.html` 和 `zotero-bridge.js`，确保迁移期间可回退。

## 前后端分离重构：阶段 2 已完成

- 建立 Vite + React + TypeScript 前端入口和 React Query 数据请求层。
- 将任务页接入 `/api/v1/workspaces/:workspaceId/tasks`，支持范围切换、读取、新增、完成切换和软删除。
- 前端任务操作携带任务 `version`，后端检测并返回 409 版本冲突。
- 旧版单文件页面仍可通过原入口访问，重构版通过 `npm run dev:web` 启动。

## 前后端分离重构：阶段 3 已完成

- 增加每日打卡、健身类型、训练计划、训练日志 SQLite 表及索引。
- 扩展旧数据导入脚本，迁移打卡和健身历史数据。
- 增加 Dashboard 汇总、今日打卡和健身查询 API。
- React 首页已展示任务完成率、打卡统计和本周训练统计，并支持打卡切换。
- React 健身页已展示计划和最近训练记录。

## 前后端分离重构：阶段 4 已完成

- 增加科研灵感、科研待办、阅读记录、学习资源和英语挑战 SQLite 表。
- 旧 JSON 的科研数据与学习数据已幂等迁移并保留扩展元数据。
- 增加科研总览、创建灵感、更新科研待办和学习中心 API。
- React 科研页支持查看灵感/待办/阅读记录并创建灵感、切换待办完成状态。
- React 学习页支持展示 AI 学习资源和英语挑战状态。

## 前后端分离重构：统一收尾阶段已完成

- 迁移内容创作数据：内容灵感、热点、新闻、内容复盘和 AI 漫剧发布记录统一存入 SQLite，并提供查询与新增内容灵感 API。
- 增加数据保护接口：支持按工作区导出表级 JSON 快照和创建 SQLite 一致性备份；备份仅保留最新 10 份。
- 重构版前端补齐内容创作页、本机集成页和全局导航；未知地址返回明确的 404 页面。
- Zotero 桥接以 `apps/zotero-agent` 受管入口运行，保持只监听本机、只读的安全边界。
- 增加 API/Web 容器构建文件、Nginx 反向代理配置和 Compose 编排；持久化数据存入独立 Docker 卷。
- 修正导出逻辑，按各表真实主键/工作区字段导出，覆盖任务关联、审计事件和全局健身类型。
- 增加共享契约的自动化测试，并完成 TypeScript、生产构建、SQLite 迁移/导入、API 回归和页面回归。

## 旧版体验等价层已完成

- 以 `creator-workspace.html` 作为唯一的视觉与行为基准，不再维持外观或交互不同的简化重构页。
- Vite 开发服务器和生产构建均原样提供 `/legacy/creator-workspace.html`；默认入口及原有领域 URL 会重定向到对应的旧版 hash 页面。
- 增加 `legacy-snapshot` API 与共享契约：完整工作区快照保存至 SQLite，写入使用独立版本号并返回 HTTP 409 冲突，避免多窗口静默覆盖。
- 首次旧 JSON 导入会初始化快照但不会覆盖已在 SQLite 中运行的旧版快照；保留 `8788` 旧入口的文件系统授权/localStorage 回退能力。
- 天气服务迁移到 Fastify API，Zotero 仍保持经本机只读 Agent 访问；旧版的界面、侧栏、命令面板、快捷键和所有页面结构保持一致。

## 验证基线

每次修改后至少执行：

```bash
node --check zotero-bridge.js
node --check <(sed -n '/<script>/,/<\/script>/p' creator-workspace.html | sed '1d;$d')
```

浏览器回归应覆盖桌面/390px 视口、hash 深链、导入非法 JSON、键盘复选框、弹窗焦点循环和本地存储写入。
